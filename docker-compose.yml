services:
  server:
    build: ./server
    ports:
      - "3000:3000"

  cobol:
    build: ./cobol
    ports:
      - "8080:8080"
    environment:
      - API_BASE_URL=http://server:3000
    depends_on:
      - server
